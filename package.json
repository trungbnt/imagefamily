{
  "name": "image-family",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && node index.js",
    "build": "cd client && npm install && npm run build",
    "install-server": "cd server && npm install",
    "install-all": "npm install && npm run install-server && cd client && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
